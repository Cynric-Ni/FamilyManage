# Spring Boot 应用配置文件
# YAML 格式，比 properties 更易读

# 应用基本信息
spring:
  application:
    name: family-management-backend  # 应用名称

# 服务器配置
server:
  port: 8080  # 服务器端口

# 日志配置
logging:
  level:
    root: INFO  # 根日志级别
    com.family.management: DEBUG  # 我们的代码使用 DEBUG 级别，方便学习调试
